// Plik wyłączony - aplikacja działa w trybie offline / static (bez AI).
// Wszystkie ćwiczenia są zdefiniowane w pliku types.ts

/*
import { GoogleGenAI, Type } from "@google/genai";
import { Exercise, FileWithPreview } from "../types";

export const generatePlan = async (...) => { ... }
*/

export {}; // Empty export to satisfy module system
